{
  "name": "nt-message",
  "version": "1.0.0",
  "description": "Supporting protobuf functions for Noita Together",
  "scripts": {
    "prepush": "npm test && npm run build",
    "test": "tsc --noEmit && npx jest",
    "build": "rm -rf dist/* && npx tsc",
    "build_win": "del /q /s dist\\* && npx tsc"
  },
  "author": "Kris Reeves",
  "license": "MIT",
  "main": "dist/index.js",
  "devDependencies": {
    "@types/jest": "^29.5.10",
    "@types/node": "^20.10.2",
    "jest": "^29.7.0",
    "protobufjs": "^7.2.5",
    "protobufjs-cli": "^1.1.2",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "long": "^5.2.3"
  }
}
